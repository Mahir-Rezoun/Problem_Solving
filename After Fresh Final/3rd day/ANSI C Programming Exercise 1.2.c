/*Compute and display area where radius is given*/
#define pi 3.1416
main()
{
    float radius,area;
    radius=10;
    area=pi*(radius)*(radius); /*u have to use * must*/
    printf("%0.3lf",area);
    return 0;
}
